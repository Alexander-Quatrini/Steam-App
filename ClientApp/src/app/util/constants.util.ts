import { environment } from 'src/environments/environment'

export const Constants = 
{
    steamCookieName: "SteamID",
    sessionIdName: "SessionID",
    apiPort: environment.API_REST_PORT,
    apiUrl: environment.API_REST_URL
};